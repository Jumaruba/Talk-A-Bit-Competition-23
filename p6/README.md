The tests of this problem were created using a python script. 
To generate random tests execute:

```
python ./generator.py
```

To test if the program p6 is correct, execute:

```
cd ..
./test.fish p6
```